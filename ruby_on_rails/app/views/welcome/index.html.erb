<!DOCTYPE html>
<html>
<head>
    <title>Welcome Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
        }
        .container {
            text-align: center;
        }
        h1 {
            color: #333;
        }
        p {
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Job posting example</h1>
        <p>This is an example job advert that can be found on a Embloy customer's web page </p>

        <%= form_tag("/api/request", method: "get") do %>
          <%= submit_tag("Apply with EMBLOY") %>
        <% end %>
    </div>
</body>
</html>